##### 'misc' directory notes

- `schema.xml` is used by the us-epigraphy solr instance. It's stored here to track version-changes.


##### DB transfer command

- `db_transfer_validation` handles database export/import/validation. See the docstring in
  `usep_app/management/commands/db_transfer_validation.py` for purpose and usage notes.

---
